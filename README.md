# Ele-Autopilot

## Commands

```bash
# 安装依赖
bun install

# 启动开发服务器
bun dev

# 生产构建
bun run build

# 启动生产服务器
bun run start

# 格式化代码
bun run format

# React DevTools 调试（独立窗口）
# !!! 必须先启动 react-devtools，然后在执行 bun dev !!!
bunx react-devtools
```
